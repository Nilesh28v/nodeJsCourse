//Arrays are one of the data strutures .to store more than 1 values we got to data structure.

var countries =new Array('India','Usa') //we can also decalre arrray like this


const name1=['saurav','nilesh','mahendra',23,345,456,true,false];
console.log(name1);
console.log(name1[2]);
console.log(name1.length); 

//push adds the new element at the end of the array;
name1.push('shubham');
console.log(name1);
console.log(name1.length);

//pop will delete the last elemetn in the array.
name1.pop()
console.log(name1);
console.log(name1.length);